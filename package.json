{
  "devDependencies": {
    "git-format-staged": "^4.0.0",
    "husky": "^9.0.11",
    "lint-staged": "^17.0.0",
    "prettier": "3.9.5",
    "@commitlint/cli": "^21.0.0",
    "@commitlint/config-conventional": "^21.0.0"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{md,json,js,ts,jsx,tsx,html}": [
      "prettier . --write --ignore-unknown"
    ],
    "*.{js,css,md}": "prettier --write"
  }
}
